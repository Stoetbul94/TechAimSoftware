# Tech Aim 0.9.0-RC1 — test results

**Recorded:** 2026-07-30 · **Build:** 0.9.0-RC1, Internal Field Test

Only results that were actually produced appear here. Everything not performed
is marked as such and is **not** converted into a pass.

---

## 1. Automated suites — all run

| Suite | Result | Baseline |
|---|---|---|
| Reliability | **2202 / 0** | 2202 / 0 ✅ |
| Training | **568 / 0** | 568 / 0 ✅ |
| Finals 10m | **143 / 0** | 143 / 0 ✅ |
| 3P Finals | **235 / 0** | 233 / 0 ✅ (+2 release-identity assertions) |
| Evidence governance | **903 / 0** | 903 / 0 ✅ |
| Manuals source checks | **1098 / 0** | 1098 / 0 ✅ |
| Project memory | **204 / 0** | 204 / 0 ✅ |
| **Deployment audit (new)** | **76 / 0** | — |
| Generated manuals | 181 / 14 | known deferred commit-stamp drift |

No test was weakened. The 3P Finals suite grew because the release-identity
assertion now also requires the field-test notice and forbids any
general-release claim in the channel string.

## 2. Release build

| | Result |
|---|---|
| Clean Qt Release build | **PASS** |
| Debug artefacts in package | none |
| Banner | `Tech Aim 0.9.0-RC1 Release build · commit <rc1> · built <date> · Internal Field Test · flavour TECH_AIM` |
| "Pre-Beta Validation" present | **no** — replaced by *Internal Field Test* |

`main.o` is force-rebuilt before packaging because the commit string is baked
in at qmake time; without that the banner reports a stale commit.

## 3. Deployment audit — PASS (76 / 0)

756 files. No source, no tests, no seeded or athlete session data, no `.git`,
no `.claude`, no build scripts, no object files, no keys, no passwords, no
developer configuration, no absolute developer paths, no Demo profile data.
Required runtime present, including the MinGW C++ runtime and the Qt Windows
platform plugin. Every top-level directory is documented in the manifest.

**Two audit checks were wrong on first run and were corrected, not relaxed:**
Qt's own QML modules legitimately ship `.qml` (only *Tech Aim* QML is
forbidden), and a bare "review" substring flagged Qt's
`CharacterPreviewBubble.qml` (the seed check is now scoped to athlete names and
data shapes).

**One real packaging defect was caught by it:** `Qt6OpenGLWidgets.dll` was
missing, and the portable package would not start without it.

## 4. Portable launch — PASS

Extracted the ZIP to a clean directory and launched with
`-UseNewEnvironment`, which resets to the machine environment. **The machine
PATH contains no Qt** (verified). The application started and stayed up.

**This is not the clean-machine test.** See §5.

## 5. Clean-machine test — NOT PERFORMED

**CLEAN-MACHINE TEST BLOCKED — PHYSICAL ENVIRONMENT REQUIRED.**

No second Windows machine without Qt development tools was available. The
portable launch in §4 is a strong indicator — the machine PATH has no Qt and
the package supplied its own runtime — but it ran on the development machine,
where a Qt installation still exists on disk and could in principle be reached
by some other mechanism.

The full 15-step clean-machine sequence (extract → launch → QML pages → home
screen → Live config → AppData → logs → athlete → session start → forced
termination → detection → recovery → new session → relaunch) **has not been
run** and must be before the weekend.

## 6. Physical target test — NOT PERFORMED

**BLOCKED — PHYSICAL TEST REQUIRED.** No Tech Aim / SETA target hardware is
attached to this machine. The full sequence is specified in
`0.9.0-rc1-field-test-plan.md` and recorded on
`0.9.0-rc1-field-test-checklist.md`.

Not performed: target connection · shot capture · score verification ·
coordinate verification · timestamp verification · sequence verification ·
disconnect/reconnect · data integrity per shot.

## 7. Recovery field test — NOT PERFORMED

**BLOCKED — PHYSICAL TEST REQUIRED.** Recovery is covered extensively by the
automated reliability suite (2202 checks, including replay, hash-chain
validation, snapshot and resume), but the *field* recovery sequence — real
shots, forced termination, restart, recover, continue — has not been run
against hardware.

## 8. Support bundle — NOT EXERCISED END TO END

`Make-SupportBundle.ps1` is written and packaged. It has **not** been run to
produce a bundle, because a meaningful bundle needs a real session and real
logs. Its sanitization rules are asserted by the deployment audit (the script
is the one packaged file permitted to mention secret-shaped words, because it
exists to strip them).

**Run it once at the range before it is needed in anger.**

---

## Open defects by severity

| Severity | Count | Detail |
|---|---|---|
| **1 — blocking** | **0 known** | No Severity 1 software defect is open. Physical testing has not been done, so hardware-class Severity 1 defects cannot yet be excluded. |
| 2 | 0 known | |
| 3 | 1 | UI-WIND-006 — the 3P session-level verdict was not visually confirmed against each position screen. Approved to remain open and documented. |

## Status

| | |
|---|---|
| **SOFTWARE PACKAGE READY** | **YES** — release build succeeds, deployment audit passes, ZIP created, SHA-256 recorded, portable launch passes, no Severity 1 software defect open. |
| **Weekend field test** | **BLOCKED — PHYSICAL TEST REQUIRED.** Clean-machine launch, target connection, shot capture, forced-termination recovery and reconnect have not been performed, and the rollback binary is not yet built. |
