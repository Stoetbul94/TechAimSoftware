# Tech Aim 0.9.0-RC3 — SETA Evaluation: freeze record

**Nothing has been sent anywhere.** The installer exists locally under
`dist/rc3/` (gitignored) and awaits Arnold's approval.

| Field | Value |
|---|---|
| Product | Tech Aim |
| Version | 0.9.0-RC3 |
| Channel | SETA Evaluation |
| Source commit | `69b84a1` |
| Installer | `TechAim-0.9.0-RC3-SETA-Evaluation-Setup.exe` |
| Installer SHA-256 | `89637B226A76CCD5836AF77AB95F858EB88AFDE6CE9589F2CEFCB4F374952340` |
| TechAim.exe SHA-256 | `11C95D7E1E76A9621B3EC39F27BA3E8BA222BFB923CA3CCDBDDD6AA05D769E3D` |
| Build timestamp (UTC) | 2026-08-15T13:11:14Z |
| Qt / compiler | 6.5.3 MinGW 64-bit / MinGW 11.2.0 |
| app_mode / developer_mode | Live / 0 |
| Payload files | 767 |
| Qt runtime deployed by | explicit allow-list (windeployqt platform-plugin detection fails on this Qt 6.5.3 MinGW install) |

**Regression at the frozen commit:** reliability 2350/0 · training 568/0 ·
10 m finals 143/0 · 3P finals 235/0 · QML 110/0 · all four documentation
checks PASS · Release build exit 0.

**Immutability.** This name and these hashes identify one artefact. If SETA
finds a release-blocking defect, the next build ships as **RC3a** or **RC4**
with new hashes — the RC3 installer is never silently replaced.

## Gate results

| Gate | Result |
|---|---|
| Source leakage | **PASS** — 0 banned files in a 767-file payload, verified independently of the packaging script |
| Proprietary loose QML | **0** — 502 staged `.qml` are Qt runtime module content; Tech Aim's 80 application QML files are embedded via `qml.qrc` |
| Personal / development data | **PASS** — 0 `.tch`, 0 logs, 0 support bundles, 0 report PDFs from development |
| Legacy Tachus assets | **PASS** — 0 in payload; six removed from `images.qrc` so they are no longer embedded |
| RC3 identity | **PASS** — `0.9.0-RC3-SETA`, channel "SETA Evaluation", notice "Evaluation Build — Not for Official Competition Results" |
| Clean first-run | **PASS** — fresh `config.ini`, per-user `AppLocalDataLocation` created only after launch |

The single approved `.ps1` in the payload is `Make-SupportBundle.ps1`, the
runtime diagnostic utility SETA needs; the leakage scan documents that
exception explicitly.

## Not done, and why

**LOCAL INSTALL TEST — NOT PERFORMED.** The installer requires elevation
(`PrivilegesRequired=admin`) and this session's shell is not elevated; a silent
install returned exit code 2 (cancelled at the UAC prompt). Security was not
weakened to force it. To run it manually, from an **elevated** PowerShell:

```
& "dist\rc3\TechAim-0.9.0-RC3-SETA-Evaluation-Setup.exe"
```

Then verify: launch is from `C:\Program Files\Tech Aim\TechAim.exe` and **not**
`release\TechAim.exe`; Settings → About shows 0.9.0-RC3 / SETA Evaluation and
the evaluation notice; disciplines, Settings and EN↔DE behave per
`docs/release/0.9.0-rc3-seta-manual-check.md`; then restart and uninstall.

**CLEAN-MACHINE TEST — PENDING.** No clean Windows machine or VM is available
here, and a development-PC install would not qualify anyway.

**PHYSICAL QUALIFICATION — PENDING SETA EVALUATION.** No live firing was
performed and no historical build's result is being reused. SETA validates FTDI
/ USB discovery, target connection, the disciplines, shots, paper feed,
disconnect, reconnect, COM re-enumeration, first shot after reconnect,
restart/recovery and first shot after recovery.

**Support-bundle audit — outstanding.** `Make-SupportBundle.ps1` ships but has
not been re-audited against the RC3 rules (no source, no repository traversal,
no unrelated personal files, application diagnostic data only, carries build
identity). It should be checked before the installer goes to SETA.
