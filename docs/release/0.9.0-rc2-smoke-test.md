# Tech Aim 0.9.0-RC2 — four-shot smoke test

**FIELD TEST — NOT FOR OFFICIAL COMPETITION RESULTS**

RC2 fixes two things and nothing else: automatic COM detection, and automatic
paper feed. Four shots is enough to prove both.

**Extract RC2 into `C:\TechAim\RC2\`.** Do not extract it over
`C:\TechAim\RC1\` or `C:\TechAim\Rollback\` — the three must stay separate.

| Field | Value |
|---|---|
| Date / operator | |
| Version shown | expect **0.9.0-RC2** |
| Channel shown | expect **Internal Field Test** |
| Commit shown | |
| Operating mode | expect **Live** |

---

## A — startup detection

| # | Check | Result |
|---|---|---|
| A1 | Target connected **before** starting RC2 | ☐ |
| A2 | Start RC2 | ☐ pass ☐ fail |
| A3 | **CH340 on COM4 is selected automatically** | ☐ pass ☐ fail |
| A4 | **Bluetooth COM5 and COM6 are ignored** | ☐ pass ☐ fail |
| A5 | Target connects without touching the COM list | ☐ pass ☐ fail |

The log records every candidate and every rejection reason. If A3 fails, note
what the log says it chose.

## B — sighter feed (shot 1)

| # | Check | Result |
|---|---|---|
| B1 | Start 10 m Air Rifle, fire one **sighter** | ☐ |
| B2 | The shot appears **exactly once** | ☐ pass ☐ fail |
| B3 | The paper motor feeds **exactly once** | ☐ pass ☐ fail |
| B4 | The feed uses the **sighter** duration | ☐ pass ☐ fail |

## C — match feed (shot 2)

| # | Check | Result |
|---|---|---|
| C1 | Fire one **counted** shot | ☐ |
| C2 | The shot appears **exactly once** | ☐ pass ☐ fail |
| C3 | The paper motor feeds **exactly once** | ☐ pass ☐ fail |
| C4 | The feed uses the **match** duration | ☐ pass ☐ fail |

Both durations are currently 1.0 s, so B4/C4 will look identical. **To prove
they differ, set the sighter duration to 2.0 s in Settings before B** and
watch the motor run visibly longer on the sighter than on the counted shot.

## D — reconnect (shot 3)

| # | Check | Result |
|---|---|---|
| D1 | Disconnect the target cable | ☐ |
| D2 | A disconnected state is shown | ☐ pass ☐ fail |
| D3 | Reconnect the cable | ☐ |
| D4 | RC2 reconnects **automatically** | ☐ pass ☐ fail |
| D5 | Fire one counted shot | ☐ |
| D6 | **One** shot, **one** feed, no duplicate | ☐ pass ☐ fail |

## E — restart (shot 4)

| # | Check | Result |
|---|---|---|
| E1 | Close RC2, restart with the target connected | ☐ |
| E2 | It connects **automatically** to the remembered adapter | ☐ pass ☐ fail |
| E3 | **No paper feeds during startup or recovery** | ☐ pass ☐ fail |
| E4 | Fire one final counted shot | ☐ |
| E5 | One shot, one feed | ☐ pass ☐ fail |

E3 matters most: feeding paper for shots recovered from the journal would be
the worst possible failure, and the code guards against it first.

---

## Optional — COM-number change

Move the adapter to a **different USB socket** so Windows assigns a new COM
number, then restart RC2. It should still find the target.

| Check | Result |
|---|---|
| New COM number assigned (note it) | |
| RC2 still recognises the adapter | ☐ pass ☐ fail |

---

## If something fails

Stop, photograph the screen, note the time, do **not** delete the session, and
run `Make-SupportBundle.ps1 -SessionId <id>` from the RC2 folder.

Roll back with `C:\TechAim\Rollback\` — it shows **0.9.0 / Pre-Beta
Validation**, so you can always tell which build is running.

**Overall:** ☐ PASS ☐ FAIL ☐ NOT COMPLETED
