# RC3a — SETA handoff folder (frozen)

**Nothing has been sent.** The folder exists locally and awaits Arnold.

## Superseded RC3 cleanup

| Item | Result |
|---|---|
| `dist/rc3/TechAim-0.9.0-RC3-SETA-Evaluation-Setup.exe` | **REMOVED** |
| `dist/rc3/TechAim-0.9.0-RC3-SETA-Evaluation-manifest.json` | **REMOVED** |
| `dist/seta-handoff/…RC3-SETA-Evaluation/` | **NOT FOUND** — never created |
| Installed copy at `C:\Users\User\Desktop\Tech Aim` | **NOT the obsolete RC3.** Its `TechAim.exe` hashes to `C108850A…E7AC`, which is **RC3a**, not RC3's `11C95D7E…9E3D`. It is the verified RC3a local install and was **left untouched** |
| `dist/rc3/…RC3a…Setup.exe` + manifest | **PRESERVED** |

RC3's hashes remain recorded in `0.9.0-rc3-freeze-record.md` and
`0.9.0-rc3a-freeze-record.md`. Only the confusable distributable was removed;
no history, documentation, defect record or source was touched.

## Handoff folder

`dist/seta-handoff/TechAim-0.9.0-RC3a-SETA-Evaluation/` — exactly four files,
no subdirectories:

| File | Size |
|---|---|
| `TechAim-0.9.0-RC3a-SETA-Evaluation-Setup.exe` | 29 459 KB |
| `SHA256.txt` | — |
| `README-SETA.pdf` | 82 KB |
| `SETA-Evaluation-Checklist.pdf` | 76 KB |

The installer was **copied, not rebuilt**. Hash re-computed from the file
actually placed in the folder:
`0E7E90AC13F80B56D5CD40AEDAF30FF0D7F7147ABD2D49BB4C95DA0DE722A916` — matches
the frozen RC3a manifest.

## Independent audit of the folder

| Check | Result |
|---|---|
| Entries | 4 files, 0 directories |
| Unapproved entries | **0** |
| Source code (`.cpp/.cc/.cxx/.h/.hpp`) | **0** |
| Proprietary loose QML | **0** |
| `.pro/.pri`/CMake | **0** |
| `.o/.obj/.pdb` | **0** |
| `.py/.bat/.cmd/.ps1` | **0** |
| `.tch`, logs, internal reports, `.md`, `.json` | **0** |
| `.git`, tests, tools, emulator, Claude/project-memory | **0** |
| Repository paths inside the PDFs | **0** |

Both PDFs were text-extracted and scanned: no "claude", "repository", "defect
register", "harness", "seta10", "qrc", ".cpp", "git" or developer path appears
in either. The checklist carries all 21 items with PASS / FAIL / NOT TESTED
plus tester, date, Windows version, target hardware, firmware and support-bundle
reference fields.

## Immutability

RC3a is frozen. If anything changes after this point it ships as **RC3b** or
**RC4** with new hashes. RC3a is never rebuilt or replaced under its own name.

## External send folder (2026-08-15)

`C:\Users\User\Downloads\TechAim-SETA-RC3a\` — the folder Arnold sends. It is a
**controlled copy**; the internal frozen archive at
`dist/seta-handoff/TechAim-0.9.0-RC3a-SETA-Evaluation/` is untouched and
remains the release of record.

```
TechAim-SETA-RC3a\
├── TechAim-0.9.0-RC3a-SETA-Evaluation-Setup.exe   (copied, not rebuilt)
├── SHA256.txt
├── README-SETA.pdf                                (regenerated - see below)
├── SETA-Evaluation-Checklist.pdf
├── English\   TechAim_Quick_Start_EN.pdf, TechAim_Operator_Manual_EN.pdf,
│              TechAim_Troubleshooting_EN.pdf
└── Deutsch\   TechAim_Quick_Start_DE_Beta.pdf, TechAim_Operator_Manual_DE_Beta.pdf,
               TechAim_Troubleshooting_DE_Beta.pdf
```

10 files, 2 directories. Installer SHA-256 re-verified from the placed copy:
`0E7E90AC13F80B56D5CD40AEDAF30FF0D7F7147ABD2D49BB4C95DA0DE722A916` — matches.

**README regenerated, deliberately.** The approved version predates the
`English\` / `Deutsch\` structure, so it could not explain the folder. It now
carries a contents table, the six-step order, and an explicit statement of the
German translation status. Nothing proprietary was added.

**German status, stated rather than implied.** All three German manuals were
regenerated at the same commit as the English set, so none is stale. They are
**beta translations awaiting native technical review**, and the filenames say
so. Per `TechAim_German_Translation_Status.md`: Quick Start is a *complete
draft*; Operator Manual and Troubleshooting are **partial**. English is the
controlled master edition, and the README tells the reader to fall back to it
where German is incomplete. Nothing was auto-translated to fill gaps.

**Independent audit:** 10 files, 2 directories, 0 banned extensions, 0 old RC3
files, exactly one installer. All eight PDFs text-extracted and scanned for
`seta10`, `.cpp`, `.qml`, `repository`, `claude`, `defect register`, `project
memory`, developer paths, `github` and `.git` — **none present in any file**.
`Make-SupportBundle.ps1` is deliberately NOT in this folder: the installer
already deploys it into the installation directory.
