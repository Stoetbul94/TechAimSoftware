# Stabilisation audit manifest (pre feature/3p-training)

Baseline: branch `chore/stabilisation` off `feature/techaim-dialogs` (5dbb363).
Scope: bug fixes + hygiene only — no features, no workflow redesign, no
scoring/match-logic refactors, licence-expiry stays disabled.

| # | Issue | File:line | Impact | Correction | Risk | Verification | Commit |
|---|-------|-----------|--------|------------|------|--------------|--------|
| S1.1 | `width = width*distance + palletSize` references non-existent `distance`; ReferenceError aborts sizing, group circle stuck at bullet size since initial import | CenterPane.qml:1116 (groupRect.refreshPosition) | Runtime error on every MPI/group refresh; broken group-circle feature | `distance` is the stale name for `group_distance` (mm, computed 40 lines up, includes one bullet dia). Replace the recursive width chain with explicit `groupPx = group_distance * offsetX` (offsetX = canvas px/mm, already computed) | Low — marker-only; no scoring | Launch; demo shots at centre + edge; MPI toggle on; circle spans widest pair, sane min/max; 10 m + 50 m; no ReferenceError in log | S1 |
| S1.2 | Bare `gameMode` (main.qml has no such property) → binding throws, inner-ten stars wrong/frozen on series PDF pages | MatchReportInfo.qml:656-657, 733-734 | Silent wrong data in Match report/PDF | Qualify as `loginPage.gameMode` (authoritative: LoginPage property, APPSETTINGS-backed; proven fix pattern from SummaryReportView) | Low — same fix already validated elsewhere | Match report preview + PDF export; rifle & pistol star thresholds; series totals unchanged | S1 |
| S1.3 | `globalMatchModel.get(listIndex).timestamp` unguarded — `get()` past count returns undefined → TypeError | MatchReportInfo.qml:610-613 (getTimeStamp) | Runtime error during report load with partial series | Guard model+range+row+field; return "—" when unavailable; never fabricate times | Low | Empty/partial/full series; sighter-only; first/last shot; saved match | S1 |
| S1.4 | `rep.executiveSummary.competitionShotCount` / `rep.recoveryAnalysis.overall.*` evaluated while COACHREPORT.report is unpopulated → TypeErrors at startup | CoachDashboardView.qml:305; CoachDetailedView.qml:361,364 (+ sibling chains) | Startup log noise; masks real errors | Add `reportAvailable` guard property per view (`rep && rep.valid === true && section !== undefined`); bind sections only when available; clean empty state; do NOT trigger analysis | Low — guards only | Startup clean; Coach window before any match shows empty state; after match renders; tab switches don't re-analyse | S1 |
| S2.1 | `qml_resouceTest.qml` compiled into binary; unused | qml.qrc:54 | Binary bloat, confusion | Remove qrc entry + delete file after reference sweep | Low | qmake+build+launch | S2 |
| S2.2 | `Seta.pro.user` (machine-specific IDE state) tracked | repo root | Dirty diffs across machines | `git rm --cached`; gitignore `*.pro.user*`, `build-*/` (`.qtc_clangd/`, `.qtcreator/` already ignored) | None | `git status` clean with IDE open | S2 |
| S2.3 | `Seta.pro` references missing `translations.qrc` → qmake warning every build | Seta.pro:52 | Build noise | No translation assets exist anywhere → Option A: remove the stale reference (documented) | Low | qmake runs with no warning | S2 |
| S2.4 | Dead tracked files: LoginPageOld.qml, RightPanel - Copy.qml, RightPanelORG.qml, Page1.qml, Page1Form.ui.qml, images/New folder.7z, 12× *_old/*_copy images | various | Repo bloat/confusion | Per-file reference sweep (QML/C++/qrc/pro/docs); delete only confirmed-unused | Low-Med (image refs need care) | Sweep output archived in commit msg; build+launch; affected pages eyeballed | S2 |
| S3 | customprint.cpp dead code: printTest(), createTablePdf() demo (dummy contacts + TestModel), printPNG unreachable body, stale includes | customprint.cpp/h | Dead weight in shipping print module | Verify no QML/C++/test callers; remove functions + header decls (+ TestModel if orphaned); working PDF paths untouched | Low-Med | PDF composer test rerun; Summary/Match/Coach/Finals export paths grep-verified untouched; build+launch | S3 |
| S4 | ~60 console.log across hot paths (per-shot logs in CenterPane/RightPanel/ShootingPage/LoginPage); assorted qDebug noise | multiple | Log flooding; LogFile drop risk | Delete obsolete; gate useful diagnostics behind existing APPSETTINGS developer mode (single flag, no new flags); keep real warnings; QLoggingCategory documented as later pass | Med (touches many files — mechanical only) | Demo shooting session produces near-silent log; dev mode restores diagnostics | S4 |
| S5 | Deprecated implicit Connections handlers (Qt 6 warnings at startup) | LoginPage, CenterPane, RightPanel, ShootingPage, SummaryReportView, others per startup log | Deprecation warnings; future Qt break | Mechanical `onFoo:` → `function onFoo(args)`; parameter names from signal signatures; batched per file | Med (signal args must match) | qmllint on changed files; launch; exercise each signal flow (login, shots, report open) | S5 |
| S6 | "current style does not support customization" — diary TextArea/TextField background customisation under native style | CoachPrintView.qml:371-409 | Startup warnings; styling ignored | Replace affected print-view inputs' customised background with style-safe wrappers (Rectangle + plain control), NOT a global style change | Low | Coach print tab: fields render, focus, diary save/load, PDF export | S6 |
| S7 | feature/techaim-dialogs unpushed (no remote); PR chain undocumented | git | Work at risk; unclear merge order | Push + upstream; document branch graph and merge order; NO merges without approval | None | git status/branch -vv; PR list | S7 |
| S8 | CLAUDE.md stale (old rebrand stage, old report architecture, no dialogs/finals docs, old branch state) | CLAUDE.md | Bad handoffs | Refresh to current architecture; drop ephemeral narration | None | Read-through | S8 |

Deferred (explicitly NOT in this phase): 3P Training, RMS, Android, new coach
analytics, licence-expiry reactivation (stays disabled; see
docs/techaim-dialogs.md for its rewritten-but-commented call), full rebrand,
SettingsPage redesign, ModConnectorDialog redesign, scoring/schema/finals-rule
changes. UI backlog (ModConnectorDialog + remaining rebrand pages) recorded in
S8 doc refresh.
